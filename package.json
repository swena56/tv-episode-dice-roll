{
  "name": "tv-episode-dice-roll",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "herokuPostBuild": "cd client && npm install && npm run build",
    "start": "node index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/swena56/tv-episode-dice-roll"
  },
  "dependencies": {
    "cors": "^2.8.4",
    "cowsay": "^1.3.1",
    "express": "^4.16.3"
  },
  "engines": {
    "node": "10.5.0"
  }
}
